<h2><%= @user.email %></h2>

<% @user.pics.each do |pic| %>
  <h4><%= pic.title %></h4>
  <p><%= image_tag pic.image.url(:large) %></p>

<% end %>


<p><%= link_to "Add Pic", new_user_pic_path(@user) %></p>

<p><%= link_to "Log out", log_out_path %></p>
